# Release v0.3.1.5 — 2026-04-19

## Summary

Patch dependency update: bumps the `md2html-tailwind4` uv source pin from `v1.2.0` to `v1.4.2`. No API changes, no migrations, no breaking changes.

---

## Changes

### Updated
| File | Change |
|---|---|
| `pyproject.toml` | `[tool.uv.sources]` rev for `md2html-tailwind4` updated from `v1.2.0` → `v1.4.2` |
| `uv.lock` | Lock file refreshed to reflect the new source revision |

---

## Compatibility Notes

- All existing integrations remain fully compatible.
- No database migrations required.
- No changes to public APIs, settings, templates, or admin configuration.
- Compatible with Python ≥ 3.11, Django ≥ 5.2.13, django-unfold ≥ 0.89.0.

---

## Validation Performed

- Tailwind CSS assets built successfully via `scripts/tailwind.sh`.
- `python manage.py makemigrations` — no changes detected.
- `uv build` — wheel and sdist produced without errors.

---

## Upgrade Guide

```bash
uv add "django-whiteneuron==0.3.1.5"
```

No configuration changes needed. Drop-in replacement for v0.3.1.4.

**Rollback**: downgrade to `django-whiteneuron==0.3.1.4` if any issue is observed.
