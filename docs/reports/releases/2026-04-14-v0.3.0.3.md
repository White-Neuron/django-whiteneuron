# Release v0.3.0.3 — 2026-04-14

## 1. Tóm Tắt Phát Hành
- **Phiên bản**: 0.3.0.3
- **Mục tiêu phát hành**: Cập nhật dependency bảo mật và patch mới nhất.
- **Phạm vi thay đổi**: Chỉ thay đổi version constraint trong `pyproject.toml`.

## 2. Thay Đổi Chính
- Nâng `pillow` từ `>=12.1.1` lên `>=12.2.0`.
- Nâng `cryptography` từ `>=46.0.5` lên `>=46.0.7`.

## 3. Tính Năng Mới
- Không có tính năng mới.

## 4. Tương Thích Và Rủi Ro
- **Đánh giá tương thích**: Cả hai dependency đều là minor/patch bump — backward compatible.
- **Rủi ro còn lại**: Rất thấp.
- **Kế hoạch giảm thiểu**: Pin lại version cũ nếu phát hiện regression từ upstream.

## 5. Xác Thực Đã Thực Hiện
- **Build Tailwind**: OK (v4.1.16 + daisyUI 5.3.8)
- **Makemigrations**: No changes detected
- **Build package (`uv build`)**: OK — wheel + sdist 1.4 MB

## 6. Hướng Dẫn Nâng Cấp
- **Bước triển khai**: `uv add django-whiteneuron==0.3.0.3 && uv sync`
- **Điều kiện rollback**: Quay về `0.3.0.2` nếu gặp lỗi.

## 7. Bảng File Thay Đổi

| File | Thay đổi |
|---|---|
| `pyproject.toml` | version bump, pillow bump, cryptography bump |
| `uv.lock` | lock file updated |

## 8. Ghi Chú
- Branch: `main2.0`
- Tag: `v0.3.0.3`
