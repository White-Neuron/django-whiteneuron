# Release v0.2.46 — 2026-04-04

## 1. Tóm Tắt Phát Hành
- **Phiên bản:** `0.2.45` → `0.2.46`
- **Mục tiêu phát hành:** Cập nhật thông tin nhận diện thương hiệu trong email template
- **Phạm vi thay đổi:** Hẹp — chỉ ảnh hưởng đến `whiteneuron/base/utils.py` (email template HTML)

## 2. Thay Đổi Chính
- **Logo email:** Thay URL logo từ `api.logo.com` sang URL Google hosted (`ci3.googleusercontent.com`) để tránh phụ thuộc bên ngoài không ổn định.
- **Tên công ty:** Cập nhật từ `White Neuron Company Limited` → `White Neuron Co., Ltd` (tên chính thức mới).
- **Chức danh:** Cập nhật từ `CEO` → `Founder & CTO`.
- **Padding email signature:** Điều chỉnh `padding` của các span trong signature cho đồng đều (`0px 0px 0px 0px` và `0px 8px 0px 8px`).
- **URL website trong email:** Cập nhật hiển thị từ `whiteneuron.ai` → `https://whiteneuron.ai` (URL đầy đủ).

## 3. Tính Năng Mới
- Không có tính năng mới.

## 4. Tương Thích Và Rủi Ro
- **Đánh giá tương thích:** Hoàn toàn tương thích ngược — không thay đổi API, model, migration hay settings.
- **Rủi ro còn lại:** Thấp. URL Google hosted cho logo email có thể bị block bởi một số email client nghiêm ngặt; không ảnh hưởng chức năng.
- **Kế hoạch giảm thiểu:** Không cần hành động bổ sung.

## 5. Xác Thực Đã Thực Hiện
- **Build Tailwind:** ✅ Passed
- **Makemigrations:** ✅ Không có migration mới
- **Build package (`uv build`):** ✅ Passed
  - `django_whiteneuron-0.2.46-py3-none-any.whl` (1.47 MB)
  - `django_whiteneuron-0.2.46.tar.gz` (1.42 MB)
- **Twine check:** ✅ PASSED (wheel + sdist)
- **Metadata:** ✅ Name=`django-whiteneuron`, Version=`0.2.46`

## 6. Hướng Dẫn Nâng Cấp
- **Bước triển khai:** Chạy `uv pip install --upgrade django-whiteneuron==0.2.46` và khởi động lại service. Không cần chạy `migrate`.
- **Điều kiện rollback:** Nếu cần, downgrade về `0.2.45` với `uv pip install django-whiteneuron==0.2.45`.

## 7. Ghi Chú
- **Nhánh:** `main2.0`
- **Tag:** `v0.2.46`
- **Files thay đổi:** `pyproject.toml`, `whiteneuron/base/utils.py`
