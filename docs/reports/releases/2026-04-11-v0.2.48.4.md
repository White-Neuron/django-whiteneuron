# Release v0.2.48.4 — 2026-04-11

## 1. Tóm Tắt Phát Hành
- **Phiên bản**: `0.2.48.3` → `0.2.48.4`
- **Mục tiêu phát hành**: Nâng cấp bảo mật các gói phụ thuộc transitive `cbor2` và `ujson`.
- **Phạm vi thay đổi**: Chỉ cập nhật `uv.lock` — không có thay đổi code logic hay schema.

## 2. Thay Đổi Chính
- `cbor2` nâng từ `v5.8.0` → `v5.9.0` — bản vá lỗi encoding/decoding CBOR.
- `ujson` nâng từ `v5.11.0` → `v5.12.0` — cải thiện bảo mật và hiệu suất JSON serialization.

## 3. Tính Năng Mới
- Không có tính năng mới.

## 4. Các File Thay Đổi

| File | Loại thay đổi |
|------|--------------|
| `uv.lock` | Cập nhật phiên bản cbor2, ujson |
| `pyproject.toml` | Bump version `0.2.48.3` → `0.2.48.4` |
| `README.md` | Thêm entry changelog v0.2.48.4 |

## 5. Tương Thích Và Rủi Ro
- **Đánh giá tương thích**: Thay đổi chỉ ở package versions — không ảnh hưởng API hay schema.
- **Rủi ro còn lại**: Rất thấp — `cbor2` và `ujson` là minor version bumps với backward compatibility đảm bảo.
- **Kế hoạch giảm thiểu**: Smoke check artifacts trước khi push tag.

## 6. Xác Thực Đã Thực Hiện
- **Build Tailwind**: ✅ Done in 154ms, no errors.
- **Makemigrations**: ✅ No changes detected.
- **Build package (`uv build`)**: ✅ `django_whiteneuron-0.2.48.4-py3-none-any.whl` + `.tar.gz` generated.

## 7. Hướng Dẫn Nâng Cấp
- **Bước triển khai**: `pip install django-whiteneuron==0.2.48.4` hoặc cập nhật `pyproject.toml`.
- **Điều kiện rollback**: Quay về `0.2.48.3` nếu gặp vấn đề với JSON hoặc CBOR serialization.

## 8. Ghi Chú
- Release này là patch bảo mật thuần túy — không yêu cầu chạy migrations hay thay đổi cấu hình.
