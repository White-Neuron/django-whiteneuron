# Release v0.3.4.13 — 2026-07-04

## Summary

Patch release: bump `md2html-tailwind4` to 1.5.3 and update lock file.

## Main Changes

### Updated
- **Dependency**: Bumped `md2html-tailwind4` from `>=1.5.2` to `==1.5.3` in `pyproject.toml` — resolves upstream fixes/improvements in the markdown-to-Tailwind HTML converter.
- **Lock file**: Refreshed `uv.lock` (476 insertions, 495 deletions) to reflect updated dependency resolution.

## Files Changed

| File | Change |
|------|--------|
| `pyproject.toml` | Dependency version bump |
| `uv.lock` | Lock file refresh |

## Compatibility Notes

- No breaking changes; safe for all v0.3.x users.
- No schema or migration changes.
- Tailwind CSS assets already built and included in package.

## Upgrade Guidance

```bash
pip install --upgrade django-whiteneuron==0.3.4.13
```

No manual migration required.

## Rollback

Safe to revert to v0.3.4.12; no schema changes introduced.
