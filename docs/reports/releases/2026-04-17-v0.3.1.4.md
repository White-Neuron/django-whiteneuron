# Release v0.3.1.4 — 2026-04-17

## 1. Release Summary
- **Version**: 0.3.1.4
- **Release goal**: Bump `md2html-tailwind4` Git source to `v1.2.0`; clean up redundant version constraint in `dependencies`.
- **Scope**: Dependency update only (`pyproject.toml`, `uv.lock`). No source code changes.

## 2. Main Changes

### Updated
- **`pyproject.toml`**:
  - `[tool.uv.sources]` rev for `md2html-tailwind4` changed from `1.1.0` → `v1.2.0`.
  - Removed redundant `>=1.1.0` version constraint from `dependencies` (version is now controlled exclusively via `[tool.uv.sources]`).
- **`uv.lock`** — lock file refreshed to reflect the new resolved dependency.

## 3. New Features
- None.

## 4. Compatibility & Risk

| Item | Assessment |
|---|---|
| Django compatibility | No change — no source code modified |
| `md2html-tailwind4` v1.2.0 | Minor version bump; no breaking changes expected |
| Rollback risk | Very low — revert `pyproject.toml` uv source rev to `1.1.0` and run `uv sync` |

## 5. Validation Performed
- **Build Tailwind**: ✅ Passed (`bash scripts/tailwind.sh`)
- **Makemigrations**: ✅ No new migrations
- **Build package (`uv build`)**: ✅ `django_whiteneuron-0.3.1.4-py3-none-any.whl` and `.tar.gz` built successfully
- **Twine check**: ✅ Both artifacts passed

## 6. Upgrade Guide
1. Update dependency to `django-whiteneuron==0.3.1.4`.
2. No migration needed.
3. No settings changes needed.
- **Rollback**: Downgrade to `0.3.1.3` — no database changes to revert.

## Files Changed

| File | Change type |
|---|---|
| `pyproject.toml` | Dependency source rev bump + constraint cleanup |
| `uv.lock` | Lock file refresh |
