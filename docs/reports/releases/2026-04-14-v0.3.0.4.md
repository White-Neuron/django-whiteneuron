# Release v0.3.0.4 — 2026-04-14

## 1. Tóm Tắt Phát Hành
- **Phiên bản**: 0.3.0.3 → 0.3.0.4
- **Mục tiêu phát hành**: Sửa lỗi 403 CSRF khi truy cập `/logout/` bằng GET trên production.
- **Phạm vi thay đổi**: Nhỏ — chỉ ảnh hưởng flow logout admin.

## 2. Thay Đổi Chính
- **Fixed**: `whiteneuron/base/sites.py` — override `logout()` trong `BaseAdminSite` để redirect GET request sang trang login thay vì trả 403 CSRF error.

## 3. Tính Năng Mới
- Không có tính năng mới.

## 4. Tương Thích Và Rủi Ro
- **Đánh giá tương thích**: Hoàn toàn tương thích ngược. POST logout (từ nút logout trong UI) vẫn hoạt động bình thường qua `super().logout()`.
- **Rủi ro còn lại**: Không có.
- **Kế hoạch giảm thiểu**: N/A.

## 5. Xác Thực Đã Thực Hiện
- Build Tailwind: OK (tailwindcss v4.1.16 + daisyUI 5.3.8)
- Makemigrations: No changes detected
- Build package (`uv build`): OK — .whl (1.49 MB) + .tar.gz (1.46 MB)

## 6. Hướng Dẫn Nâng Cấp
- **Bước triển khai**: `uv pip install django-whiteneuron==0.3.0.4` — không cần migration hay thay đổi settings.
- **Điều kiện rollback**: Quay về v0.3.0.3 nếu có bất kỳ vấn đề nào với flow logout.

## 7. Files Changed

| File | Thay đổi |
|------|----------|
| `pyproject.toml` | Version bump 0.3.0.3 → 0.3.0.4 |
| `whiteneuron/base/sites.py` | Override `logout()` — GET → redirect to login |
| `README.md` | Changelog entry v0.3.0.4 |
